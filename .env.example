# Local app tokens
VITE_GITHUB_TOKEN=
VITE_LINEAR_TOKEN=

# Optional Sentry browser config
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
VITE_SENTRY_RELEASE=

# Deployment / CI secrets are stored in Vercel or GitHub Actions, not committed here.
# Keep these names reserved so the repo and docs stay aligned.
VERCEL_TOKEN=
VERCEL_ORG_ID=
VERCEL_PROJECT_ID=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
